# Steam-KaKaBase 路线图与版本记录

这里记录已发布版本、当前限制与后续路线。Catalog enrich 是持续运行的数据任务，不是后续开发的阻塞条件。

## v0.8.2：近期史低、热门榜扩展与路由恢复

- “更多”菜单新增三个只读 SQLite 价格池：24 小时内达到历史最低价、低于此前本站记录的新史低、以及好评率至少 90% 的史低游戏。
- 今日史低与新史低池要求至少 500 条评测或 500 名当前在线玩家，并优先按在线人数、评测数排序；高好评史低池要求至少 1,000 条评测，优先按评测数、在线人数排序。这样不会让少量 100% 评测的条目挤占榜单。
- 新史低要求当前国区折扣价低于此前任一本站快照或已缓存 ITAD 史低；它同时属于史低，但以粉色价格标签与普通绿色史低、蓝色普通折扣区分。
- 热门榜和愿望单新增“仅查看新史低”筛选；普通史低筛选仍会包含新史低。
- 热门榜默认目标由 Top 100 提升至 Top 200。Steam 官方端点会请求 200 条；若上游仍限制为 100 条，使用 24 小时内本站已观测的不同游戏补足，且在页面明确标记为“本站观测”，不会冒充官方排名。
- 价格池与新筛选仅查询本地 SQLite，不会在页面访问时触发 Steam/ITAD 请求或创建 crawler 任务。
- Catalog enrich 的每日默认额度提升至 2,000；生产环境已连续运行 24 小时，未见额度、稳定性或磁盘异常。
- 刷新网页时会在账号状态恢复后再解析 URL 路由：管理员监控页不再被短暂的匿名状态重定向回首页；首页、热门榜、小众池、三个史低池与游戏详情也均保持各自 URL 页面。

## v0.7.5：愿望单同步与详情缓存刷新

- 修复页面认证恢复与愿望单加载竞态：已登录用户刷新详情页后，当前游戏的收藏按钮和“想买/观望/已拥有”状态会立即与账号愿望单同步。
- 详情页在可见且停留时每 60 秒重新读取一次本地 SQLite 缓存；crawler 后续写入的价格、在线人数、评测等数据会自动呈现，无需手动刷新页面。
- 自动详情更新不会发出兴趣 POST，不会唤醒 Steam 或 ITAD 采集，也不会改写浏览器历史或把用户从其他页面拉回详情页。
- 收藏游戏改为只补齐缺失的采集任务，已完成任务不会再因收藏操作被重新置为待采集；管理员明确触发的手动刷新仍保留强制刷新能力。

## v0.7.4：在线图布局与数量缩写

- 在线人数纵轴在 10k 及以上使用紧凑的 `k` 单位，降低七位数字标签的宽度。
- 调整在线图左右留白以保持柱形图区居中，同时为最右侧两行时间标签预留空间；自定义悬浮提示坐标同步更新。

## v0.7.3：在线人数图标签修复

- 为在线人数图的七位数纵轴标签和价格图的币种纵轴标签保留更宽的左侧绘图区，并同步修正自定义悬浮提示的坐标计算，避免标签被容器边缘裁切。

## v0.7.2：愿望单价格排序修复

- 收藏摘要返回用于排序的国区原始金额；按“价格最低”会稳定按免费、低价、高价排列，缺少价格快照的游戏始终放在最后。

## v0.7.1：愿望单侧栏布局修复

- 愿望单游戏条目改为固定的标题、摘要、价格提示三层，避免状态、折扣和最近本站低价时间互相挤压。
- 侧栏筛选改为规整的两列网格，并为更高的筛选区预留滚动空间；窄屏仍使用单栏页面布局。

## v0.7.0：愿望单体验

- 收藏侧栏升级为愿望单：展示当前国区价格、折扣、本站史低，以及本站最低价最近一次出现的本地时间。
- 可组合筛选“仅打折”“仅本站史低”，并可按收藏状态、最近添加、折扣、价格或在线人数查看。
- 每个账号的愿望单状态可同步为“想买、观望、已拥有”；未登录用户保留浏览器本地版本，旧本地条目会默认归为“想买”。
- SQLite schema v12 为既有 `user_favorites` 自动补充状态列和查询索引；升级仍走事务迁移与迁移前备份，已有收藏不会丢失。
- 状态更新使用现有登录 Cookie、CSRF、按 IP 收藏限流、严格枚举校验与用户 ID 条件更新；不会触发 Steam/ITAD 请求，也不会暴露他人愿望单。

## v0.6.5：长期历史采集与受限游戏支持

- 非热门玩家与国区价格历史覆盖提升至每天各 7,500 次，背景候选池为 8,000；目标约一周轮转 5 万主要游戏。
- 热门榜刷新与历史覆盖预算分离；预算按上海当天时间逐步放行，避免部署或重启后突发耗尽。
- Steam Store 改为逐请求随机错峰，crawler 按周期起点调度，保持单路采集并避免慢批次后的额外空等。
- Deadlock（1422450）使用 Steam 官方热门榜记录在线人数，免费价格作为显式快照保存；不将普通接口失败伪造成 0 人。
- 登录失败统一最短响应时间并在弹窗内显示错误，降低用户名枚举与暴力猜测价值。
- README 改为传统使用与运维文档；本文件承担版本记录和未来路线。

## 当前运行与保留策略

- crawler 每天自动压缩玩家/价格历史、清理过期任务与推荐、轮转日志并限制图片缓存；不依赖手动磁盘清理。
- 本地每日 SQLite 备份保留 14 份，异地备份由 systemd 每天执行并默认保留 30 天；当前不做日备份合并为周备份，每份都可直接恢复。
- 当数据库增长到数 GB 后，依据管理员监控中的磁盘增长曲线评估日/周/月备份分层与更短的历史保留期。

## 能力基线

已经具备：

- Vue 3 + ECharts 单页界面。
- 搜索、收藏、详情、热门榜和小众池。
- Steam Top 100 与基于持久化游标逐步建立的全量轻量 Catalog。
- 在线人数、价格、评价、发售信息和 ITAD 史低采集。
- SQLite WAL、批量写入、任务队列和历史数据压缩。
- Steam API、Steam Store、ITAD、图片 CDN 独立冷却。
- 直连优先、失败后代理回退和直连探测冷却。
- 首页三项内容每日 00:10 快照，小众宝藏与史低推荐均在 7 天内去重。
- FastAPI + Uvicorn Web 服务，以及 `/health`、`/ready` 健康检查。
- GitHub Actions、密钥泄漏检查和前端 JavaScript 语法检查。
- pytest 单元及进程内 API 集成测试，测试不访问真实外部服务。
- 基于版本号的事务式数据库迁移、升级前自动备份、手动恢复工具，以及玩家/价格历史压缩和过期记录清理测试。
- Playwright 桌面与移动端流程测试，覆盖导航、榜单筛选、搜索和收藏往返。
- Web 与 crawler 已拆为独立进程；SQLite 租约和心跳阻止两个 crawler 同时调度采集。
- crawler 启动时会恢复崩溃遗留任务并清理旧榜单代际任务；队列、心跳、限流冷却和数据库文件大小等运行数据仅在受保护的管理员监控接口中提供。

当前已知限制：

- `config.py` 已独立解析环境变量；`utils.py`、`pricing.py` 和 `logging_utils.py` 已拥有无副作用的时间、名称、价格、URL 脱敏、缓存清理及日志文件工具；`steam_client.py` 已拥有 HTTP、代理回退、Steam/ITAD 服务冷却、图片下载安全校验与端点解析，且不再导入 `_runtime.py`；`db.py` 已拥有连接、事务、迁移初始化、schema 验证、任务队列和主要只读查询，也不再反向导入 `_runtime.py`；`game_queries.py` 已拥有游戏详情、热门榜、追踪列表、搜索索引、目录占位详情和 API 序列化；`catalog.py` 已拥有 AppList 扫描和 enrich；`crawler.py` 已接管热门榜、玩家、商店预览和 ITAD 史低编排；首页推荐业务已迁入 `services.py`；`game_commands.py` 拥有无网络副作用的追踪写命令。Web、crawler 与 crawler 进程的遗留调用均经过 `runtime_compat.py` 这一个兼容边界，不再直接导入 `_runtime.py`。
- v0.5.0 已改为账号级同步收藏；未登录收藏保留在本机浏览器，不能跨设备同步。生产部署强制 HTTPS，账号 Cookie 使用 Secure；认证与收藏接口按 IP 限流，账号可自行删除并清除其同步数据。
- v0.6.0 新增仅管理员可见的监控页：匿名访问聚合、服务资源、crawler、数据库、备份、恢复演练和脱敏日志集中展示。普通用户不能读取运行指标；只有服主能维护管理员名单，并可触发固定的本地/异地备份、隔离恢复演练和 Web/crawler 重启。健康摘要会提示 5xx、心跳、备份、演练和磁盘异常。
- v0.6.1 修复服主控制组件在 systemd `NoNewPrivileges` 沙箱下无法调用 sudo 的问题，改为权限受限的本机 Unix Socket 控制代理；网页确认弹窗替代浏览器原生弹窗，手动本地备份会更新监控时间。
- v0.6.2 修复公网账号注册/登录的网关错误处理：Nginx 429 或临时 5xx 不再被误报为后端端口错误；前端会安全处理结构化校验响应，并在提交期间禁用确认按钮以避免重复请求触发限流。认证仍采用 Nginx 与应用内双层按 IP 限流。
- v0.6.3 完成本轮 runtime 边界收口：Web、crawler 调度器和独立 crawler 进程不再直接导入 `_runtime.py`；剩余旧编排 API 集中经过 `runtime_compat.py`，追踪/取消追踪的无网络写入由 `game_commands.py` 负责。外部采集仍仅由 crawler 执行，SQLite 队列、事务、429 冷却、代理回退和备份语义保持不变。
- v0.6.4 新增 SQLite 分层历史覆盖：热门、账号收藏、近期详情兴趣与受限后台候选按优先级积累在线人数和中国区价格；每日玩家/价格采集预算防止 Catalog 扩大后失控，外部请求保持单路错峰。v11 的 `game_activity` 只保存游戏级时间信号，不保存访客身份；详情 GET 仍只读缓存，现有兴趣 POST 仅提升队列和覆盖优先级。
- 登录失败统一在至少 0.5 秒后返回，且无论用户名是否存在都执行同等的密码派生；配合每 IP 10 次 / 5 分钟认证限流，降低暴力猜测和用户名枚举风险。认证弹窗内直接显示错误，不再被遮罩层挡住。
- 历史覆盖预算调整为每天约 7,500 个非热门玩家样本和 7,500 个非热门中国区价格样本；背景候选池扩展至 8,000，目标让约 5 万主要游戏在一周内完成首轮或一次轮转。预算随上海当天时间逐步放行，避免 crawler 启动突发耗尽。热门榜的玩家与价格刷新使用独立任务，不再挤占历史覆盖额度；本地搜索本身不产生外部请求，打开详情只记录游戏级兴趣信号并在背景覆盖中优先处理。Steam Store 采集改为逐请求随机间隔，而非仅限制并发数，保留 429 冷却与单路执行；调度器按周期起点计时，慢的串行批次不会再额外空等一整分钟。
- Deadlock（1422450）加入受控例外：其普通 Store/玩家接口受限时，免费价格作为显式本地事实每日写入一次；在线人数仅在 Steam 官方热门榜返回该 App 时记录，每 15 分钟一次。不会将普通请求失败误写为 0 人，也不依赖第三方数据源。
- 当前不收集邮箱，故没有自助修改密码/找回密码；服主可在受保护监控页按用户名重置任意账号密码，重置会注销该账号全部旧会话。普通管理员没有此权限；接入邮件服务前仍需先评估隐私说明、发信域名和邮件成本。
- Vue 和 ECharts 已固定为本地 vendor 文件；仍需在真实弱网和移动设备上完成体验验收。
- 图表悬浮、等宽快照柱、横轴标签数量和热门榜滚动已有 Playwright 覆盖；仍缺少更多窄屏断点与长期数据量下的视觉回归。
- 本机迁移前、部署前和每日 SQLite 备份已具备；Vultr Object Storage 异地备份已配置，下一步是定期核对上传结果并完成恢复演练。

## v0.1.1：稳定本地版

目标：停止扩大核心文件，建立可重复验证的本地版本。

### 测试

- 扩充数据库损坏和并发升级场景测试；迁移中断、回滚和本地恢复已有自动化覆盖。
- 扩充玩家与价格历史在月末、闰年和多地区数据下的压缩测试。
- 验证进程重启后 `running` 任务能恢复为可执行状态。
- 验证 Steam Store 429 不会暂停 ITAD，反向也一样。
- 验证代理不可用、直连恢复和半开探测行为。
- 为图表切换、tooltip 和热门榜滚动加载增加 Playwright 测试。
- 继续扩充桌面与移动视口的文字溢出、图表空白和布局重叠检查。

### 工程化

- 维持 GitHub Actions：Python 编译、pytest、前端脚本语法检查。
- 维持 API Key 泄漏检查，确保 `.env`、SQLite、日志和缓存不入库。
- 增加 `LICENSE`、`CHANGELOG.md` 和基础 Issue 模板。
- 将版本号集中到单一配置，不再使用日期字符串作为程序版本。
- 为关键 API 响应建立稳定的契约测试。

完成标准：测试稳定通过，本地连续运行 3 天没有任务失控、数据库锁死或日志爆量。

## v0.2.0：后端模块化与可部署基础

目标：把 Web 请求与后台采集解耦，为服务器部署做准备。

### 模块拆分

- `config.py`：真正拥有环境变量解析和配置校验。
- `db.py`：拥有连接、迁移初始化、schema 验证、查询仓储、事务和任务队列；不得反向依赖 `_runtime.py`。
- `game_queries.py`：拥有只读游戏查询、搜索缓存与面向 API 的稳定序列化；不得发起外部请求或依赖 `_runtime.py`。
- `storage_maintenance.py`：拥有日志轮转、价格/玩家快照压缩、旧任务清理和图片缓存清理；不得依赖 `_runtime.py`。
- `crawler_data.py`：拥有热门榜、ITAD、价格、评论和元数据的 SQLite 批量写入与到期任务选择；不得依赖 `_runtime.py`。
- `crawler_fetch.py`：拥有 Steam 玩家、AppDetails 和评论的受限并发批量请求；必须复用 `steam_client.py` 的代理、重试与服务冷却。
- `catalog.py`：拥有 AppList 游标扫描、增量 enrich、非游戏分类和小众池写入；不得反向依赖 `_runtime.py`。
- `steam_client.py`：拥有 Steam、ITAD、图片请求与分服务冷却。
- `crawler.py`：拥有任务认领、优先级、调度和失败恢复。
- `services.py`：只组合数据库操作，不直接发外部请求。
- `server.py`：只处理参数、状态码和 JSON。
- 逐步缩小并最终移除 `_runtime.py` 兼容实现。

每迁移一个模块都必须先通过编译、单元测试和本地 API 冒烟测试，避免一次性重写。

### 服务化

- 扩充 FastAPI API 契约测试和统一错误响应。
- [已完成] 将 Uvicorn Web 进程与 crawler 调度进程彻底分离。
- [已完成] Web 进程只读缓存并投递任务，不同步等待 Steam。
- [已完成] 独立 crawler 进程负责采集，SQLite 租约确保同一数据库只有一个调度器。
- 增加 `/health`、`/ready` 和受保护的管理状态接口。
- 为管理操作增加鉴权，禁止公网任意触发全量刷新。

完成标准：Web 服务和 crawler 可以独立重启，页面始终读取旧缓存，采集失败不会拖垮 API。

## v0.3.0：私有测试服务器

目标：先只供自己访问，验证真实服务器环境。

推荐起步配置：

- Ubuntu VPS，2 核 CPU、2～4 GB 内存、30～50 GB SSD。
- 日本、新加坡或香港节点，实际选择以 Steam 请求成功率为准。
- Caddy 或 Nginx 提供 HTTPS 和反向代理。
- systemd 或 Docker Compose 管理 Web 与 crawler。
- SQLite 继续使用 WAL，并确保只有一个写入调度器。

上线前需要完成：

- 每日自动备份 SQLite，至少保留 7 个日备份和 4 个周备份。
- 已于 2026-09-11 完成 Vultr Object Storage 异地备份到新目录的恢复演练：恢复副本为 schema v8、`PRAGMA quick_check=ok`、游戏数 4,529；未触碰生产数据库。
- 为日志、数据库大小、任务积压、429 和请求失败率设置监控。
- 限制图片缓存总量、文件大小、来源域名和请求超时。
- 将 CORS 从 `*` 收紧到正式域名。
- API Key 只保存在服务器环境变量或密钥文件中。

私有环境至少运行 7 天，记录：

- Steam API、Steam Store 和 ITAD 成功率。
- 每日 429 次数及冷却后恢复情况。
- Catalog enrich 实际吞吐与重复请求比例。
- SQLite 和图片缓存每日增长量。
- Web/crawler 重启恢复和备份可用性。

## v0.4.0：公开测试

目标：允许少量外部用户访问，同时控制资源和数据风险。

公开前必须决定收藏策略：

- 简单方案：收藏只保存在用户浏览器 `localStorage`。
- 完整方案：增加用户账户和按用户隔离的收藏表。

不能继续使用当前全局 `games.tracked` 作为多用户收藏状态。

还需要：

- 对搜索、详情、图片代理和管理接口设置频率限制。
- 为 API 增加分页、最大 limit 和输入校验。
- 禁止任意 URL 进入图片代理，只允许 Steam 官方图片域名。
- 准备隐私说明、数据更新时间说明和服务限制说明。
- 核对 Steam Web API、Steam Store 与 ITAD 的使用条款。
- 页面明确显示玩家数约 30 分钟更新、价格与评价每日更新。
- 使用真实移动设备和至少两种桌面浏览器完成验收。

完成标准：公开测试连续运行 2～4 周，服务可恢复、数据可备份、接口没有明显滥用。

## v1.0.0：稳定公开版本

`v1.0.0` 不以功能数量判断，而以稳定承诺判断：

- 安装、升级、备份和恢复流程有文档且经过验证。
- API 响应格式有版本边界，不随意破坏兼容性。
- Web 与 crawler 失败后可以独立恢复。
- 多用户收藏不会互相污染。
- 关键后端逻辑、API 契约和主要前端流程都有测试。
- 不再依赖临时兼容核心 `_runtime.py`。
- 公开测试阶段发现的高优先级问题已经关闭。

## Catalog enrich 与开发节奏

不需要等待全量 Catalog 全部 enrich 完成才能继续开发。AppList 扫描与详情 enrich 相互独立，扫描每天按批次推进并在完成后每周校验一次；DLC、Demo、工具、软件等会在 AppDetails 分类后进入排除终态。

可以立即并行进行：

- 后端物理拆分。
- CI、测试和 API 契约建设。
- FastAPI/Uvicorn 迁移。
- 收藏多用户方案设计。
- 部署脚本、健康检查、备份与恢复工具。
- 前端端到端测试和响应式修复。

需要等待较大样本后再判断：

- 小众池候选数量是否合理。
- 近 8 年、10 人在线和峰值 2,000 的门槛是否需要调整。
- 加权公式是否偏向评测量过大的游戏。
- 每日 2,000 条 enrich 的真实成功率和 Steam 限流压力。

按理论额度，30,000 条需要约 15 天完成一轮尝试，但“尝试”不等于“成功补全”：无商店数据、DLC、下架应用和请求失败都会产生跳过或重试。开发工作应继续推进，只需让采集器在后台稳定运行，并在调整推荐算法前使用阶段性样本验证。

## 下一步建议顺序

1. 连续观察线上至少 7 天：记录 crawler 心跳、任务积压、Steam/ITAD 429、SQLite/WAL 大小、异地备份成功情况和 HTTPS 证书续期状态；再为异常阈值增加告警。这是当前最高优先级。
   - v10 已提供仅管理员可见的监控面板和服主专用的固定控制组件；后续可增加更细的任务状态与告警通知。
2. 补记本次异地备份的远端文件名、上传时间与文件大小，和恢复结果一并留档。
3. 在线上完整 crawler 周期稳定后，将 `runtime_compat.py` 中仍被调用的编排、状态与小众池 API 逐项物理迁移，并最终删除 `_runtime.py`；兼容边界不得新增业务逻辑。
4. 扩展 Playwright 的窄屏断点、长历史数据图表和账号删除流程；用真实手机和两种桌面浏览器验收。
5. 账号只保持“同步收藏”的轻量定位。若要做邮箱找回、邮箱验证或第三方登录，先确定邮件服务、隐私文本、发信域名和运维成本；当前管理员交互式重置已足够。
