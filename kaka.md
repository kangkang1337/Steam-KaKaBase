我建议不要急着公开上线，而是走“先可验证、再可部署、最后公开”的路线。当前 `v0.1.0` 已经是完整的本地原型，下一阶段重点应该从加功能转向稳定性和工程化。

**总体路线**

```text
v0.1.1 稳定本地版
   ↓
v0.2.0 可部署后端
   ↓
v0.3.0 私有测试服务器
   ↓
v0.4.0 公开测试
   ↓
v1.0.0 稳定公开版本
```

**阶段一：v0.1.1 测试与清理**

先暂时停止增加大功能，建立测试基础。

优先补这些单元测试：

- 价格换算、史低允许 0.5 元误差
- 折扣、免费、国区无售价的显示逻辑
- 小众池筛选、加权评分和近五年限制
- `crawl_tasks` 的 `pending/retry/done/not_available`
- 429 冷却、404 永久不可用、超时重试
- 玩家历史压缩和数据清理策略
- 游戏名称占位符处理

集成测试使用临时 SQLite：

- 数据库首次初始化和旧数据库迁移
- 收藏、取消收藏后重新启动仍然正确
- 热门榜读取不触发外部请求
- 空价格和零评价不会无限重新入队
- 服务重启后 `running` 任务能够恢复

前端端到端测试：

- 搜索并打开游戏
- 收藏与取消收藏
- 热门榜筛选和排序
- 小众池不足 20 条时全部展示
- 图表切换游戏后不残留旧数据
- 桌面和手机尺寸不重叠

建议使用 `pytest`、`httpx MockTransport` 和 Playwright。

**阶段二：v0.2.0 后端生产化**

这是上线前最关键的一步。

目前的 Python `ThreadingHTTPServer` 适合本地调试，不适合公网。建议升级为：

- FastAPI：API 和参数校验
- Uvicorn：生产 Web 服务
- 独立 crawler 进程：负责定时采集
- Web 进程只读取缓存和投递任务
- SQLite 继续使用 WAL，但只能有一个 crawler 调度器

同时继续迁移 `_runtime.py`：

- 真正把配置移入 `config.py`
- 请求实现移入 `steam_client.py`
- SQL 和迁移移入 `db.py`
- 采集实现移入 `crawler.py`
- 最终删除 `_runtime.py` 中重复的 Handler 和 main

这个阶段还要增加：

- `/health`：进程是否正常
- `/ready`：数据库是否可用
- 管理接口鉴权
- API 请求频率限制
- 固定依赖版本
- 数据库自动备份与恢复脚本
- CORS 从 `*` 改为正式域名

**阶段三：GitHub 工程化**

在 GitHub 建立自动检查：

- 每次 push 自动运行 Python 编译
- 自动运行 pytest
- 检查 `.env` 和密钥泄露
- 检查前端基础语法
- Pull Request 合并前必须通过测试

建议增加：

- `LICENSE`：MIT
- `CHANGELOG.md`
- `CONTRIBUTING.md`
- Issue 模板
- `dev` 和 `feature/*` 分支工作流
- GitHub Actions

以后版本号只在一个位置维护，不再使用日期字符串作为程序版本。

**阶段四：v0.3.0 私有服务器**

不建议使用 GitHub Pages、Vercel 或普通 Serverless，因为项目有 SQLite、定时任务和长期采集进程。

比较合适的是：

- Ubuntu VPS
- 2 核 CPU
- 2～4 GB 内存
- 30～50 GB SSD
- 日本、新加坡或香港节点
- Caddy 或 Nginx 提供 HTTPS
- systemd 或 Docker Compose 管理进程

先只允许自己访问，运行至少 3～7 天，观察：

- Steam 请求成功率
- 429 出现频率
- 代理是否真的需要
- SQLite 锁等待
- 数据库每日增长量
- 日志增长量
- crawler 重启恢复
- 备份能否成功还原

**阶段五：v0.4.0 公开测试**

满足以下条件再公开：

- 连续七天没有采集任务失控
- 429 后能真正暂停
- 所有页面错误都有降级显示
- 数据库可以从备份恢复
- API Key 已重新生成且只保存在服务器环境变量
- 搜索、热门榜、收藏和详情测试通过
- 公网接口有限流
- Steam 和 ITAD 使用方式符合其 API 条款

公开测试阶段不要承诺实时数据，可以明确标注“玩家数约 30 分钟更新，价格每日更新”。

**v1.0.0 标准**

我建议至少公开测试两到四周后再发布 `v1.0.0`。它不代表功能很多，而是代表：

- 安装和部署流程稳定
- 数据不会轻易损坏
- 失败后能恢复
- API 格式相对稳定
- 基本测试覆盖完整
- 不再依赖临时兼容核心

当前最合理的下一项工作是创建 `v0.1.1` 测试基础：先搭建 `pytest`、临时 SQLite 和 Steam 请求模拟，再覆盖任务队列、价格与收藏这三块风险最高的逻辑。完成后再继续物理拆分 `_runtime.py`。